# Copy to .env and fill in real values. Never commit .env.

# --- Core ---
NODE_ENV=development
# 5001 because macOS AirPlay Receiver squats on port 5000
PORT=5001

# MongoDB Atlas connection string (Database > Connect > Drivers)
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/nordcart?retryWrites=true&w=majority

# --- Auth ---
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# --- CORS ---
# The Next.js frontend origin (production: your Vercel URL). CORS allows
# exactly this origin, with credentials, so the httpOnly JWT cookie travels.
CLIENT_URL=http://localhost:3000

# --- Cloudinary (admin product-image uploads) ---
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# --- Stripe test mode ---
STRIPE_SECRET_KEY=sk_test_...
